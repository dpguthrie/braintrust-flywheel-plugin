# Examples

Examples are grouped by skill. They are templates to copy into caller repositories, not supported reusable harnesses.

| Skill | Examples |
|---|---|
| `bt-flywheel` | [`bt-flywheel/flywheel-caller.yml`](bt-flywheel/flywheel-caller.yml), [`bt-flywheel/integrations.md`](bt-flywheel/integrations.md) |

Add new examples under `examples/<skill-name>/` so each skill stays independently navigable.
