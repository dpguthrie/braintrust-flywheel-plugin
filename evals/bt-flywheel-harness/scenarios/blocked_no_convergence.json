{
  "id": "blocked_no_convergence",
  "fixture": "blocked_no_convergence",
  "goal": "Exit cleanly when repeated flywheel iterations do not converge",
  "task": "Investigate a project that has already gone through three attempted flywheel iterations. Scores oscillate without durable improvement, and one attempted prompt change was reverted after regressions. Decide whether more local changes are justified and emit the handoff.",
  "bt": {
    "routes": [
      {
        "argv_prefix": ["sql"],
        "contains": ["experiments"],
        "stdout_json": {
          "rows": [
            {"experiment_id": "exp-001", "combined_score": 0.52, "change": "dataset examples"},
            {"experiment_id": "exp-002", "combined_score": 0.49, "change": "prompt update reverted"},
            {"experiment_id": "exp-003", "combined_score": 0.51, "change": "scorer tightening"}
          ],
          "summary": "Scores oscillated between 0.49 and 0.52 across three iterations."
        }
      },
      {
        "argv_prefix": ["view"],
        "contains": ["experiment-exp-003"],
        "stdout": "experiment-exp-003: no durable improvement; two previously passing traces regressed during the reverted prompt attempt."
      }
    ]
  },
  "acceptance": [["python3", "-c", "import ast, pathlib; ast.parse(pathlib.Path('src/agent.py').read_text())"]],
  "expected": {
    "outcomes": ["no_convergence", "blocked"],
    "required_next_step_intents": ["investigate"],
    "required_terms": ["three prior iterations", "no durable improvement", "human diagnosis"],
    "required_bt": [
      {"argv0": "sql", "contains": ["experiments"]},
      {"argv0": "view", "contains": ["experiment-exp-003"]}
    ],
    "allowed_changed_paths": [
      "bt-flywheel-summary.json",
      "bt-flywheel-narrative.md"
    ],
    "forbidden_changed_paths": ["src/*", "scorers/*", "evals/*"]
  }
}
