# Offline Support Agent Fixture

This is a tiny placeholder codebase used by the `bt-flywheel` Harbor eval.
The task is to run the flywheel against the local Braintrust project, not to improve
this local agent implementation directly.
